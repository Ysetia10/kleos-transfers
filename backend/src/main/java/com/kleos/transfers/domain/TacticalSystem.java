package com.kleos.transfers.domain;

/**
 * Coarse attacking / build-up system for a manager-season appointment.
 */
public enum TacticalSystem {
    POSSESSION,
    TRANSITION,
    DIRECT,
    BALANCED
}
