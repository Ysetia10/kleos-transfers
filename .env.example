# Copy this file to .env for local-only configuration when services are introduced.
# Never commit secrets, tokens, production credentials, or private data.

# PostgreSQL connection settings; intentionally left blank.
DATABASE_URL=
DATABASE_USERNAME=
DATABASE_PASSWORD=

# Optional HTTP port override.
SERVER_PORT=
